#include "render_graph.h"

render::RenderGraph::RenderGraph(const DeviceConfiguration& device_cfg, const Extent& extent, const Image& presentation_image)
{

}

render::RenderGraph::~RenderGraph()
{
}
